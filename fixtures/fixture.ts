/*import {test as basetest} from '@playwright/test';
//import { LoginPage } from '../tests/Pages/LoginPage/LoginPage';
//import { ProductPage } from '../tests/Pages/productPage';

// ده وظيفته اني في التست مش كل شويه اعمل import {test, execpt } from playwright 
// أيضا كل object من الكلاس بدل مكتبها في كل تست بكتبها هنا 


//type pages={
    //loginPage: LoginPage;
  //  productPage: ProductPage;
//}

//const testPages=basetest.extend<pages>({
    //loginPage: async({page}, use)=>{
      //  await use(new LoginPage(page))

    //},


    //productPage: async({page}, use)=>{
    //    await use(new ProductPage(page))

  //  },

//})

//export const test = testPages;
//export const expect = testPages.expect*/